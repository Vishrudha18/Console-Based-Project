# Console-Based-Project
This is a simple console-based Appointment Management System built using Core Java and MySQL JDBC. The project allows users to manage appointments efficiently by providing CRUD operations directly through the terminal.  The application connects to a MySQL database and stores appointment information such as client name, date, time, and purpose.
